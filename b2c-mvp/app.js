const pathButton=document.getElementById('learningPathButton');const pathDrawer=document.getElementById('pathDrawer');const closePathButton=document.getElementById('closePathButton');const backdrop=document.getElementById('drawerBackdrop');const companionToggle=document.getElementById('companionToggle');const companionPanel=document.getElementById('companionPanel');const avatar=document.getElementById('socratesAvatar');const voiceButton=document.getElementById('voiceButton');function setDrawer(open){pathDrawer.hidden=!open;backdrop.hidden=!open;pathButton.setAttribute('aria-expanded',String(open));}pathButton.addEventListener('click',()=>setDrawer(pathDrawer.hidden));closePathButton.addEventListener('click',()=>setDrawer(false));backdrop.addEventListener('click',()=>setDrawer(false));companionToggle.addEventListener('click',()=>{const next=companionToggle.getAttribute('aria-pressed')!=='true';companionToggle.setAttribute('aria-pressed',String(next));companionPanel.hidden=!next;avatar.classList.toggle('state-listen',next);});voiceButton.addEventListener('click',()=>{const active=voiceButton.dataset.active==='1';voiceButton.dataset.active=active?'0':'1';voiceButton.textContent=active?'🎤 생각 말하기':'■ 듣고 있어요';avatar.classList.toggle('state-listen',!active);});
